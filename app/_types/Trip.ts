export interface IFullTrip {

    id: number,
    name: String,
    country: String,
    start_date: String,
    end_date: String,
}

export interface ITrip {
    
    name: string,
    country: string,
    start_date: string,
    end_date: string,
}